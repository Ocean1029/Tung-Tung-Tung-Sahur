# 🏙️ Town Pass (通通通通)

台北通 App - 集章系統專案

> 一個結合 NFC 技術的城市探索應用，讓用戶透過跑步活動收集地點徽章，體驗台北城市之美。

---

## 📚 專案資源

### 🎨 設計文件
- **Figma 設計稿**: [查看設計](https://www.figma.com/design/LdqLdZwYTTXuQpvpkJzOrt/%E9%80%9A%E9%80%9A%E9%80%9A%E9%80%9A?node-id=1-2&t=dzy67YEOVXsAkAG6-1)

### 📖 專案文件
- **Google Docs**: [專案發想、分工文件](https://docs.google.com/document/d/1SIkJgET925s7sBVYUhLn7tcre8B-_hKaHptOVJA48E8/edit?usp=sharing)



> ✅ **後端已部署**：前端預設連接到此 API，無需啟動本地後端即可開發前端。

---

## 📁 專案結構

```
Tung-Tung-Tung-Sahur/
├── frontend/              # Flutter 前端應用
│   ├── lib/              # Dart 程式碼
│   │   ├── page/        # 頁面元件
│   │   ├── service/     # 服務層
│   │   ├── util/        # 工具類別
│   │   └── bean/        # 資料模型
│   ├── android/          # Android 平台配置
│   ├── ios/              # iOS 平台配置
│   └── assets/           # 靜態資源
│
├── backend/              # Node.js 後端 API 服務
│   ├── src/             # TypeScript 程式碼
│   ├── prisma/          # 資料庫 Schema 和遷移
│   └── scripts/         # 工具腳本
│
└── docs/                 # 專案文件
```

---

## 🛠️ 技術棧

### 前端
- **框架**: Flutter + Dart
- **狀態管理**: GetX
- **HTTP 客戶端**: http
- **地圖**: Google Maps Flutter
- **NFC**: nfc_manager
- **定位**: geolocator

### 後端
- **運行環境**: Node.js + TypeScript
- **框架**: Express.js
- **資料庫**: PostgreSQL
- **ORM**: Prisma
- **API 文檔**: Swagger/OpenAPI

---

## 🚀 快速開始

### 前置需求

**必須：**
- **Flutter SDK**: >=3.3.0
- **Xcode**: (macOS 用戶，用於 iOS 開發)

**可選：**
- **Node.js**: >=18.18.0（僅在需要本地開發後端時）
- **Android Studio**: (用於 Android 開發)

> 💡 **提示**: 後端已部署，前端開發**不需要**安裝 Node.js 或啟動本地後端。

### 1. 克隆專案

```bash
git clone <repository-url>
cd Tung-Tung-Tung-Sahur
```

### 2. 前端設置

```bash
# 進入前端目錄
cd frontend

# 安裝 Flutter 依賴
flutter pub get

# iOS 專用：安裝 CocoaPods 依賴
cd ios
pod install
cd ..
```

### 3. 後端設置（可選）

> 💡 **注意**: 後端已經部署在雲端，**通常不需要啟動本地後端**。前端預設連接到部署的後端 API。

**只有在以下情況才需要設置本地後端：**
- 需要修改後端代碼
- 需要測試後端功能
- 需要連接到本地資料庫

**設置本地後端：**

```bash
# 進入後端目錄
cd backend

# 安裝 Node.js 依賴
npm install

# 生成 Prisma Client
npm run prisma:generate

# 啟動開發伺服器
npm run dev
```

本地後端會在 `http://localhost:3000` 啟動。

**切換到本地後端：**
如果需要在 Xcode 中使用本地後端，請在 Scheme 的環境變數中設定：
```
--dart-define=API_BASE_URL=http://localhost:3000
--dart-define=RUN_CITY_API_BASE_URL=http://localhost:3000
```

---

## 📱 iOS 開發指南（Xcode）

### 在 Xcode 中打開專案

```bash
cd frontend
open ios/Runner.xcworkspace
```

⚠️ **重要**: 必須打開 `.xcworkspace` 檔案，而不是 `.xcodeproj` 檔案！

### Xcode 常用快捷鍵

| 快捷鍵 | 功能 | 說明 |
|--------|------|------|
| `⌘ + Shift + K` | **Clean Build Folder** | 清理構建快取，解決編譯問題 |
| `⌘ + B` | **Build** | 構建專案（不運行） |
| `⌘ + R` | **Run** | 構建並運行應用 |
| `⌘ + Shift + 2` | **Devices and Simulators** | 查看連接的設備和模擬器 |
| `⌘ + .` | **Stop** | 停止運行中的應用 |

### 詳細操作步驟

#### 1. 清理專案（Clean Build Folder）

**何時使用：**
- 遇到編譯錯誤
- 依賴更新後
- 切換分支後
- 構建出現奇怪問題時

**步驟：**
1. 在 Xcode 中，選擇選單：**Product** > **Clean Build Folder**
2. 或使用快捷鍵：`⌘ + Shift + K`
3. 等待清理完成（通常幾秒鐘）

**注意：** 這會刪除所有構建產物，下次 Build 會重新編譯。

#### 2. 構建專案（Build）

**何時使用：**
- 檢查代碼是否有錯誤
- 驗證構建配置
- 不需要運行，只需要構建時

**步驟：**
1. 確保選擇正確的 Scheme（通常是 **Runner**）
2. 選擇目標設備（模擬器或實體設備）
3. 選擇選單：**Product** > **Build**
4. 或使用快捷鍵：`⌘ + B`
5. 在底部狀態欄查看構建進度

**構建成功標誌：**
- 狀態欄顯示 "Build Succeeded"
- 沒有紅色錯誤訊息

#### 3. 運行應用（Run）

**何時使用：**
- 想要在設備或模擬器上運行應用
- 測試功能時

**步驟：**
1. **選擇目標設備**
   - 點擊頂部工具列的設備選擇器（Runner 旁邊）
   - 選擇：
     - **模擬器**（例如：iPhone 15 Pro）
     - **實體設備**（你的 iPhone）

2. **選擇 Scheme**
   - 確保選擇 **Runner** Scheme

3. **運行應用**
   - 選擇選單：**Product** > **Run**
   - 或使用快捷鍵：`⌘ + R`
   - 或點擊左上角的播放按鈕 ▶️

4. **等待構建和安裝**
   - Xcode 會自動構建專案
   - 然後安裝到選定的設備
   - 應用會自動啟動

#### 4. 檢查設備連接狀態

**何時使用：**
- 想要查看連接的 iPhone
- 檢查設備是否被識別
- 查看設備資訊

**步驟：**
1. 選擇選單：**Window** > **Devices and Simulators**
2. 或使用快捷鍵：`⌘ + Shift + 2`
3. 在左側選擇 **Devices** 標籤
4. 查看連接的設備列表

**設備狀態說明：**
- ✅ **Connected**: 設備已連接且可用
- ⚠️ **Preparing**: 設備正在準備中
- ❌ **Not Trusted**: 需要信任電腦（見下方說明）

#### 5. 信任開發者證書（首次安裝）

**何時需要：**
- 第一次在 iPhone 上運行應用時
- 使用免費 Apple ID 開發時
- 看到「未受信任的開發者」提示時

**在 iPhone 上操作：**

1. **運行應用後，iPhone 會顯示提示**
   - 如果應用無法打開，會顯示「未受信任的開發者」

2. **前往設定**
   - 打開 iPhone **設定** App
   - 進入 **一般** > **VPN 與裝置管理**
   - 或 **一般** > **裝置管理**（舊版 iOS）

3. **信任開發者**
   - 找到你的開發者帳號（顯示為 Email）
   - 點擊進入
   - 點擊 **信任 "[你的 Email]"**
   - 確認信任

4. **返回應用**
   - 回到主畫面
   - 點擊應用圖示
   - 現在應該可以正常打開了

**注意：**
- 使用免費 Apple ID 時，證書有效期為 7 天
- 7 天後需要重新運行應用（會自動重新簽名）
- 或重新信任開發者

#### 6. 設定簽名（Signing）

**何時需要：**
- 第一次打開專案時
- 切換開發者帳號時
- 遇到簽名錯誤時

**步驟：**

1. **選擇 Target**
   - 在 Xcode 左側導航欄，點擊最上方的藍色專案圖示
   - 選擇 **Runner** Target

2. **進入 Signing & Capabilities**
   - 選擇 **Signing & Capabilities** 標籤

3. **選擇 Team**
   - 在 **Team** 下拉選單中：
     - 如果已有帳號：選擇你的開發者帳號
     - 如果沒有：點擊 **Add an Account...**
       - 輸入你的 Apple ID
       - 登入後選擇該帳號

4. **自動簽名**
   - Xcode 會自動：
     - 創建開發證書
     - 設定 Bundle Identifier
     - 配置 Provisioning Profile

5. **檢查狀態**
   - 應該看到綠色的勾選標記 ✅
   - 如果看到錯誤，按照提示解決

#### 7. 設定環境變數（API URL）

**如果你的應用需要連接到後端 API：（應該是不用拉）**

1. **在 Xcode 中設定**
   - 點擊頂部的 Scheme 選擇器（Runner 旁邊）
   - 選擇 **Edit Scheme...**

2. **添加環境變數**
   - 選擇左側的 **Run**（或 **Archive**）
   - 選擇 **Arguments** 標籤
   - 在 **Arguments Passed On Launch** 區域：
     - 點擊 **+** 添加：
       ```
       --dart-define=API_BASE_URL=https://run-city-424370484311.asia-east1.run.app
       --dart-define=RUN_CITY_API_BASE_URL=https://run-city-424370484311.asia-east1.run.app
       ```

3. **儲存設定**
   - 點擊 **Close**

### 常見問題排除

#### 問題 1: 構建失敗 - "No such module"

**解決方法：**
```bash
cd frontend/ios
pod install
cd ..
```
然後在 Xcode 中：`⌘ + Shift + K` 清理，再 `⌘ + B` 構建

#### 問題 2: 簽名錯誤

**解決方法：**
1. 在 Xcode 中：**Signing & Capabilities**
2. 選擇正確的 **Team**
3. 確保 **Bundle Identifier** 是唯一的

#### 問題 3: 設備未顯示

**解決方法：**
1. 確保 iPhone 已解鎖
2. 在 iPhone 上點擊「信任這部電腦」
3. 檢查 USB 連接線
4. 使用 `⌘ + Shift + 2` 檢查設備狀態

#### 問題 4: 應用無法打開

**解決方法：**
1. 前往 iPhone **設定** > **一般** > **VPN 與裝置管理**
2. 信任開發者證書
3. 重新運行應用

#### 問題 5: 依賴問題

**解決方法：**
```bash
cd frontend
flutter clean
flutter pub get
cd ios
pod deintegrate
pod install
cd ..
```

---

## 📝 開發流程

### 1. 開始開發（使用部署的後端）

**最簡單的方式 - 直接運行前端：**

```bash
# 前端會自動連接到部署的後端 API
cd frontend
flutter run
```

或使用 Xcode：
```bash
cd frontend
open ios/Runner.xcworkspace
# 按 ⌘ + R 運行
```

> ✅ **後端已部署**，前端預設連接到：`https://run-city-424370484311.asia-east1.run.app`

### 2. 使用本地後端開發（可選）

**只有在需要修改後端時才需要：**

```bash
# 終端 1: 啟動本地後端
cd backend
npm run dev

# 終端 2: 啟動前端（連接到本地後端）
cd frontend
flutter run --dart-define=API_BASE_URL=http://localhost:3000 --dart-define=RUN_CITY_API_BASE_URL=http://localhost:3000
```

或在 Xcode 中設定環境變數（見上方說明）。

### 3. 在 Xcode 中開發（iOS）

1. 打開專案：`open ios/Runner.xcworkspace`
2. 選擇目標設備（模擬器或實體設備）
3. 按 `⌘ + R` 運行
4. 開始開發！

### 4. 熱重載

- **Flutter**: 在終端按 `r` 重新載入，`R` 重新啟動
- **Xcode**: 修改代碼後，按 `⌘ + R` 重新運行

---

## 🧪 測試

### 運行測試

```bash
cd frontend
flutter test
```

### 檢查代碼品質

```bash
cd frontend
flutter analyze
```

---

## 📦 構建發布版本

### iOS

```bash
cd frontend
flutter build ios --release
```

然後在 Xcode 中：
1. 選擇 **Product** > **Archive**
2. 等待構建完成
3. 選擇 **Distribute App**

### Android

```bash
cd frontend
flutter build apk --release
# 或
flutter build appbundle --release
```

---

## 🔗 相關連結

- **Figma 設計**: [設計稿](https://www.figma.com/design/LdqLdZwYTTXuQpvpkJzOrt/%E9%80%9A%E9%80%9A%E9%80%9A%E9%80%9A?node-id=1-2&t=dzy67YEOVXsAkAG6-1)
- **專案文件**: [Google Docs](https://docs.google.com/document/d/1SIkJgET925s7sBVYUhLn7tcre8B-_hKaHptOVJA48E8/edit?usp=sharing)
- **API 文檔**: [API 文件](https://run-city-424370484311.asia-east1.run.app/docs/)
- **Flutter 文件**: [Flutter Docs](https://docs.flutter.dev/)
- **GetX 文件**: [GetX Docs](https://pub.dev/packages/get)

---

## 👥 開發團隊

- **前端工程師**: 負責 `frontend/` 目錄
- **後端工程師**: 負責 `backend/` 目錄

---

## 📄 授權

詳見 `frontend/LICENSE`

---

## 🆘 需要幫助？

如果遇到問題：

1. 檢查 [Flutter 文件](https://docs.flutter.dev/)
2. 查看專案的 `docs/` 目錄
3. 檢查 Xcode Console 的錯誤訊息
4. 使用 `flutter doctor` 檢查環境配置

---

## 📝 更新日誌

### v0.0.12
- 初始版本
- 支援 NFC 地點收集
- 跑步活動追蹤
- 徽章系統

---

**Happy Coding! 🚀**
